{
  "name": "${{ values.name }}",
  "version": "0.1.0",
  "private": true,
  "description": "${{ values.description }}",
  "scripts": {
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "start": "node dist/index.js",
    "validate": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "~5.8.0",
    "@types/node": "^20.0.0"
  }
}
