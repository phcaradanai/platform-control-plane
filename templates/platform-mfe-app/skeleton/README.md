# ${{ values.title }}

${{ values.description }}

Generated by the **Platform MFE Application** Backstage template
(`platform-mfe-app`) on the `platform-control-plane` App Factory.

## What was generated

A minimal, buildable TypeScript application foundation:

- `src/index.ts` - placeholder entry point
- `package.json`, `tsconfig.json` - build/typecheck scripts
- `catalog-info.yaml` - Backstage Software Catalog registration
- `platform-app.json` - machine-readable record of this application's
  identity, mode, owner, and requested capabilities
- `.github/workflows/ci.yml` - install, typecheck, build on every push/PR

This is a foundation, not the final application. No Module Federation
runtime, Nx generators, or Super App shell integration are included yet.

## Selected capabilities

Mode: **${{ values.mode }}**
Lifecycle: **${{ values.lifecycle }}**

Requested capabilities (recorded in `platform-app.json`, not yet
implemented):

${{ values.capabilities | dump }}

## What is a placeholder

- `platform-app.json` `runtime.status` is `not-configured` - no Module
  Federation host/remote wiring exists yet.
- None of the requested capabilities above are installed or configured.
  They are recorded so the later Nx composition phase knows what to add.
- There is no deployment configuration.

## Running validation

```bash
npm install
npm run typecheck   # tsc --noEmit
npm run build        # tsc
```

The same steps run in CI on every push and pull request to `main`
(`.github/workflows/ci.yml`).

## What comes later

Runtime Module Federation loading, Nx-based capability composition, the
Super App shell, authentication/RBAC enforcement, and deployment are out
of scope for this generated foundation and are planned for a later App
Factory phase.
