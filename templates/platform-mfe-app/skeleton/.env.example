# ${{ values.title }}
# No runtime environment variables are required yet - this application
# foundation has no configured runtime (see platform-app.json). Variables
# will be added here when Module Federation / capability runtime
# integration lands in a later phase.
