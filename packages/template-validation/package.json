{
  "name": "template-validation",
  "version": "0.0.0",
  "main": "dist/index.cjs.js",
  "types": "src/index.ts",
  "private": true,
  "description": "Validation and tests for the platform-control-plane catalog model, docker-compose config, and the platform-mfe-app software template",
  "backstage": {
    "role": "node-library"
  },
  "scripts": {
    "build": "backstage-cli package build",
    "lint": "backstage-cli package lint",
    "test": "backstage-cli package test",
    "clean": "backstage-cli package clean"
  },
  "dependencies": {
    "@backstage/plugin-scaffolder-common": "^2.2.1",
    "nunjucks": "^3.2.4",
    "yaml": "^2.4.0"
  },
  "devDependencies": {
    "@backstage/cli": "^0.36.4",
    "@types/nunjucks": "^3.2.6"
  }
}
